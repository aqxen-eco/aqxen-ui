name: upscale

services:
  pg:
    image: postgres:16
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - '5433:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=upscale

volumes:
  db_data:
